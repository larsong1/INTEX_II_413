@model IEnumerable<string>

<div class="d-grid gap-2">
    <div class="dropdown">
        <button class="btn btn-outline-secondary dropdown-toggle" style="width: 100%" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
            Filter by Category
        </button>
        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton">
            <li>
                <a class="dropdown-item @(string.IsNullOrEmpty(ViewBag.SelectedProductCategory) ? "active" : "")" asp-action="Product" asp-controller="Home" asp-route-productCategory="" asp-route-pageNum="1">
                    All Categories
                </a>
            </li>
            @foreach (string pt in Model ?? Enumerable.Empty<string>())
            {
                <li>
                    <a class="dropdown-item @(pt == ViewBag.SelectedProductCategory ? "active" : "")" asp-action="Product" asp-controller="Home" asp-route-productCategory="@pt" asp-route-pageNum="1">
                        @pt
                    </a>
                </li>
            }
        </ul>
    </div>
</div>
